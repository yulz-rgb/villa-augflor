Folder: `/Users/lana/Projects/villa-augflor-static-live` (also `Projects/villa-augflor-static-live` in Cursor)

What’s inside: fixed `index.html` + `images/` (no WordPress URLs, no Airbnb CDN).

Your one step: put these two items on GitHub (drag-drop in the repo on github.com, or `git add` + `git push` from Terminal). Cursor here can’t run `git` on your Mac (`.git/hooks` blocked).

See `DO.txt` for the same line.
